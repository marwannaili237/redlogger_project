name: Minimal Build Test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: kivy/buildozer:1.5.0 # The problematic line

    steps:
    - name: Simple Test
      run: echo "Hello from container!"
